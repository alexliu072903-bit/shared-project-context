interface:
  display_name: "Project Publisher"
  short_description: "Relate real work to goals and maintain shared goal state"
  default_prompt: "Use $project-publisher to review current goal alignment and state."
policy:
  allow_implicit_invocation: true
