interface:
  display_name: "Project Publisher"
  short_description: "Publish personal work changes into shared project state"
  default_prompt: "Use $project-publisher to decide whether this work change should update a shared project."
policy:
  allow_implicit_invocation: true
